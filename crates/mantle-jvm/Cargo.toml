[package]
name = "mantle-jvm"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[features]
gate-a-direct-attachment = []

[dependencies]
jni = "0.22.4"
mantle-audio = { version = "0.0.0", path = "../mantle-audio" }
mantle-core = { version = "0.0.0", path = "../mantle-core" }
mantle-media = { version = "0.0.0", path = "../mantle-media" }

[dev-dependencies]
loom = "=0.5.6"

[lints]
workspace = true
